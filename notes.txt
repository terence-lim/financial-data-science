# Creates a new ssh key, using the provided email as a label
ssh-keygen -t rsa -b 2048 -C "tl2735@columbia.edu"

# Set your name and email
git config --global user.name "Terence Lim"
git config --global user.email "tl2735@columbia.edu"

# Let's get pretty colored output!
git config --global color.ui auto

# Pick your editor of choice to edit commit messages (not code)
# Note that you should pick an editor that is installed on your machine
git config --global core.editor "emacs -nw"

# LF Normalization (Linux & MacOS)
git config --global core.autocrlf input

# Push Setting
git config --global push.default simple

# List local git configuration options
git config --list

cat ~/.ssh/id_rsa.pub


git init
git add README.md
git commit -m "first commit"
#git remote add origin https://github.com/terence-lim/investment-data-science.git
git remote add origin git@github.com:/terence-lim/investment-data-science.git
git push -u origin master
